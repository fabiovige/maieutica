# FIX-001: Problemas de Pacientes, Denver e Continuidade de Prontuários

**Data:** 2026-03-23
**Status:** Investigação concluída - Aguardando decisão de implementação
**Reportado por:** Usuário (profissional clínico)

---

## Problemas Reportados

1. **Todos os pacientes estão salvos como "crianças"** - adultos não aparecem ao buscar paciente para prontuário, a não ser que selecione "Criança"
2. **Todos aparecem com checklist aberto para o Denver** - apenas crianças até ~6 anos deveriam estar no Denver
3. **Prontuários sem continuidade** - ao evoluir um caso, os registros anteriores não aparecem

---

## Investigação Detalhada

### PROBLEMA 1: Adultos cadastrados como Crianças

#### Situação atual no banco de dados

| Categoria | Quantidade |
|-----------|------------|
| **Total na tabela `kids`** | 117 |
| Crianças elegíveis ao Denver (≤ 6 anos) | 43 (37%) |
| Crianças/adolescentes acima de 6 anos (7-17) | 56 (48%) |
| **Adultos (≥ 18 anos) na tabela `kids`** | **18 (15%)** |
| Total na tabela `users` com `allow=1` | 40 |
| Registros na tabela `professional_user_patient` | **0** |

#### Adultos encontrados na tabela `kids` (exemplos)

| Nome | Nascimento | Idade |
|------|------------|-------|
| MARIA DE FATIMA MACHADO | 1959-08-10 | 66 anos |
| JULIANA LUCHIARI | 1978-05-29 | 47 anos |
| ADRIANA APARECIDA DA SILVA | 1981-05-18 | 44 anos |
| LIVIA MOREIRA DANTAS LINS | 1982-04-12 | 43 anos |
| ERIKA QUEIROZ LOPES NAMBA | 1987-11-28 | 38 anos |
| IZABELA MUNIZ DA SILVA | 1998-05-19 | 27 anos |
| EDUARDO BLASSIOLI GALLEGO | 2002-08-05 | 23 anos |
| ANA BEATRIZ DE CARVALHO CUNHA | 2003-07-28 | 22 anos |
| ... e mais 10 registros | | 18-21 anos |

#### Causa raiz

O sistema possui **duas entidades** para pacientes:
- **`kids`** (crianças) - tabela dedicada com `birth_date`, `gender`, `ethnicity`, `photo`
- **`users`** com `allow=true` (adultos) - reutiliza a tabela de usuários do sistema

**Porém:** O cadastro de crianças (`KidsController`) **não valida idade**. Qualquer pessoa pode ser cadastrada como "criança" independente da data de nascimento. Como o cadastro de "Criança" é o fluxo mais familiar para os profissionais, **todos os pacientes acabam sendo cadastrados lá**, inclusive adultos.

#### Por que adultos não aparecem nos prontuários

O sistema de prontuários (`MedicalRecordsController`) busca pacientes em **duas listas separadas**:

```php
// Busca crianças
$kids = $this->getKidsForUser();     // Tabela kids

// Busca adultos
$users = $this->getUserPatientsForUser();  // Tabela users WHERE allow=1
```

Na interface, o dropdown de tipo de paciente oferece:
- **"Criança"** → lista da tabela `kids` (onde estão TODOS, incluindo adultos)
- **"Adulto"** → lista da tabela `users` com `allow=1`

**O profissional cadastrou os adultos na tabela `kids`**, então eles só aparecem quando seleciona "Criança". A opção "Adulto" busca na tabela `users`, que está vazia para esses pacientes.

#### Agravante: `professional_user_patient` vazia

A tabela pivot `professional_user_patient` (que vincula profissionais a pacientes adultos) tem **0 registros**. Isso significa que mesmo que existissem adultos na tabela `users`, profissionais sem permissão `medical-record-list-all` **não os veriam**, pois o scope `forAuthProfessional()` depende dessa tabela pivot.

---

### PROBLEMA 2: Todos com checklist aberto para Denver

#### Situação atual

| Métrica | Valor |
|---------|-------|
| Checklists ativos | 18 |
| Todos no nível | 4 |
| Abertos (situation='a') | ~14 |
| Finalizados (situation='f') | ~4 |

#### Causa raiz

**Não existe validação de idade para criação de checklists.** O Denver (Teste de Triagem do Desenvolvimento Denver II) é projetado para crianças de 0 a 6 anos, mas:

1. **`ChecklistController@create()`** lista TODAS as crianças via `Kid::getKids()` sem filtro de idade
2. **`ChecklistRequest`** valida apenas campos obrigatórios (`kid_id`, `level`) - nenhuma validação de idade
3. **Modelo `Checklist`** não tem scopes nem validação baseada em idade
4. **Competências** possuem percentis em meses (`percentil_25/50/75/90`), mas são usados apenas para exibição, não para restrição

#### Contexto importante

Das 117 crianças cadastradas, **apenas 43 (37%) têm 6 anos ou menos** e seriam elegíveis ao Denver. As outras 74 aparecem no dropdown de criação de checklist mesmo sem faixa etária adequada.

**Nota:** Atualmente, dos pacientes >6 anos, apenas 2 têm checklists (THEO LIMA VALLIM, 18 anos, 1 checklist; Maria Flor Lima, 11 anos, 1 checklist). O problema é mais de **exposição indevida** do que de uso incorreto generalizado.

---

### PROBLEMA 3: Prontuários sem continuidade

#### Situação atual

**A tabela `medical_records` tem 0 registros.** O sistema de prontuários ainda não foi efetivamente utilizado em produção.

No entanto, a infraestrutura existe parcialmente:

#### O que EXISTE no código

| Feature | Status | Detalhes |
|---------|--------|----------|
| Tabela com versionamento | Implementada | `parent_id`, `version`, `is_current_version` |
| Relação polimórfica | Implementada | `patient_type` + `patient_id` |
| Scope `currentVersion()` | Implementado | Filtra apenas versão corrente |
| Scope `forAuthProfessional()` | Implementado | Filtra por profissional |
| Método `getAllVersions()` | Implementado | Retorna todas as versões |
| Método `history()` no controller | Implementado | Sem rota mapeada |
| Método `newVersion()` no controller | Implementado | Sem rota mapeada |

#### O que NÃO EXISTE

| Feature | Status | Impacto |
|---------|--------|---------|
| **Histórico na tela de criação** | Não implementado | Profissional cria prontuário "no escuro", sem ver registros anteriores |
| **Histórico na tela de visualização** | Não implementado | Cada registro é isolado, sem link para anteriores |
| **Rota para history()** | Não mapeada | Método existe no controller mas inacessível |
| **Rota para newVersion()** | Não mapeada | Método existe no controller mas inacessível |
| **View de histórico** | Não existe | Template Blade não foi criado |
| **Visão centrada no paciente** | Não implementada | Não há página "todos os prontuários do paciente X" |
| **Link prontuário→kid/show** | Não existe | Página da criança não mostra prontuários |

#### Fluxo atual vs. fluxo desejado

**Atual:**
```
Profissional → Lista prontuários (filtro manual) → Cria novo → Não vê histórico
```

**Desejado:**
```
Profissional → Seleciona paciente → Vê todos os registros anteriores → Cria novo com contexto
```

---

## Resumo dos Achados

| # | Problema | Causa Raiz | Severidade |
|---|---------|------------|------------|
| 1 | Adultos só aparecem como "Criança" | Foram cadastrados na tabela `kids` (sem validação de idade). A tabela `users`/`professional_user_patient` está vazia para esses pacientes | **Alta** - Impede uso correto do sistema |
| 2 | Todos elegíveis ao Denver | Sem validação de idade na criação de checklist. `Kid::getKids()` retorna todos | **Média** - Exposição indevida mas uso incorreto é baixo |
| 3 | Prontuários sem continuidade | Histórico não implementado na UI. Métodos existem no controller mas sem rotas/views | **Alta** - Feature essencial não funcional |

---

## Decisões Necessárias Antes de Implementar

### Sobre os adultos na tabela `kids`:

1. **Migrar os 18 adultos da tabela `kids` para `users`?**
   - Risco: podem ter checklists, planos ou dados associados
   - Alternativa: manter onde estão e tratar "kid" como "paciente" genérico

2. **Adicionar validação de idade no cadastro de crianças?**
   - Se sim, qual o limite? 18 anos? 6 anos?
   - E os que já estão cadastrados acima do limite?

3. **Ou renomear o conceito?** Em vez de "Criança"/"Adulto", usar "Paciente" unificado?

### Sobre o Denver:

4. **Filtrar crianças >6 anos do dropdown de checklist?**
   - Hard block (impedir) ou soft warning (avisar)?
   - E os 2 checklists já criados para >6 anos?

### Sobre a continuidade:

5. **Implementar histórico na tela de criação de prontuário?**
   - Sidebar com registros anteriores?
   - Ou página separada "Linha do tempo do paciente"?

6. **Integrar prontuários na página da criança (`kids/show`)?**
   - Aba ou seção com lista de prontuários?

---

## Arquivos Relevantes

| Arquivo | Responsabilidade |
|---------|------------------|
| `app/Http/Controllers/MedicalRecordsController.php` | CRUD prontuários, métodos history/newVersion |
| `app/Http/Controllers/KidsController.php` | CRUD crianças (sem validação idade) |
| `app/Http/Controllers/ChecklistController.php` | CRUD checklists (sem filtro idade) |
| `app/Models/MedicalRecord.php` | Scopes, versionamento, polimorfismo |
| `app/Models/Kid.php` | `getKids()`, `getAgeAttribute()` |
| `app/Http/Requests/ChecklistRequest.php` | Validação checklist (sem idade) |
| `resources/views/medical-records/create.blade.php` | Form criação prontuário |
| `resources/views/medical-records/index.blade.php` | Lista prontuários com filtros |
| `resources/views/checklists/create.blade.php` | Form criação checklist |
| `database/migrations/2025_12_28_*_professional_user_patient.php` | Pivot profissional↔adulto |
